<%= form_with(model: theatre_movie, local: true) do |form| %>
  <% if theatre_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theatre_movie.errors.count, "error") %> prohibited this theatre_movie from being saved:</h2>

      <ul>
        <% theatre_movie.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :theatre_id %>
    <%= form.text_field :theatre_id %>
  </div>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.text_field :movie_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
